# T1 Notes

- Replace the condensed GitGuardex README with the user-provided long-form README copy.
- Add the exact VS Code Source Control screenshot to the visual explanation section so the workflow reads as a concrete example, not only an illustration.
- Keep command names, companion-tool references, and release-note sections aligned with current GitGuardex branding.
