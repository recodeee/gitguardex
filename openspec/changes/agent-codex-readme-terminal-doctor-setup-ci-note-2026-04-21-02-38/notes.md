# T1 Notes

- Add the current `gx` terminal status/help output as a visual README reference.
- Explain the intended entry points for `gx setup` and `gx doctor` in plain language near the top of the README.
- Pin the optional CR workflow action to an immutable commit so GitHub does not reject the workflow before startup.
- Keep the README secret instructions aligned with the actual workflow input (`secrets.OPENAI_API_KEY`).
